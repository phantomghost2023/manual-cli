# Verify stamps are local-only by convention: they are machine state, not truth.
.manual/state.json

# Generated by `manual report` / `manual serve`.
.manual/report.html
.manual/serve.pid

# Undo snapshots for accepted proposals.
.manual/undo/
