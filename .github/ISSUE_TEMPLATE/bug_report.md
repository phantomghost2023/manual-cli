---
name: Bug report
about: Something is stamped wrong, crashed, or behaved contrary to the manual
labels: bug
---

**What happened**

<!-- e.g. "verify stamped tests.unit fresh although the suite fails" -->

**Command and output**

```bash
# the exact command and its output (or --json output)
```

**Claim file** (if applicable)

```markdown
<!-- the claim, or a minimal version of it -->
```

**Environment**

- OS / Node version:
- manual-cli version (or commit):

**Fuzz-related?** Include the failing input (first 80 chars are enough) — the suite is seeded, so it will reproduce.
